const User = require("../models/user");
const GiftList = require("../models/giftList");

const getGiftListById = (req) => {};

const addGiftList = (req) => {};

const updateGiftList = (req) => {};

const deleteGiftList = (req) => {};

module.exports = {
  getGiftListById,
  addGiftList,
  updateGiftList,
  deleteGiftList,
};
